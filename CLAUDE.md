# Marketing Dashboard — session law (auto-loaded)
LIVE on Railway (project "Marketing Dashboard") + its Postgres.

## Session law (all IHG repos)
1. If you can reach `~/Desktop/Full_IH_Project`: read `_CONTEXT.md` "STATE OF PLAY" + ACTIVE LANES — parallel sessions are normal; know your lane.
2. Branch; Mimi merges (or authorizes in-chat — QUOTE her in the merge commit).
3. `git fetch && git status` first; a dirty tree you did not cause = stop and report.
4. Push REJECTED = STOP: re-read lanes / ask "who else is pushing?" before rebasing. Never merge another session's branch in passing.
5. Secrets never committed · comp data never · live systems never go down · verify before claiming done (node/py syntax check minimum).
Deeper law: THE_FABLE_METHOD.md + START_HERE_CLAUDE.md in the synced project folder.

## Facts that bite
- External sends / client-facing publishing = HUMANS ONLY, forever (standing marketing law).
- Postgres here is in the Brain Box nightly backup set — schema changes stay idempotent.
